# FYD Default

Default corporate public theme for FYD CMS.

Edit SCSS tokens in `scss/_design-tokens.scss` and Vue templates under `js/`. This folder is the canonical source; a matching copy is pre-installed under `themes/fyd-default/`.
